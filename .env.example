# Copy to .env and fill in to enable live LLM calls.
# If ANTHROPIC_API_KEY is left blank, the AI Engine automatically runs in
# deterministic "fallback mode" so the whole prototype still works offline.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-opus-4-8
DATABASE_URL=sqlite:///./skillsphere.db
